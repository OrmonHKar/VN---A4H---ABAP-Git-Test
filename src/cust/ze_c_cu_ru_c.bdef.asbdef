projection;
strict ( 1 );
use draft;

define behavior for ZE_C_CU_RU_C alias CURUC
use etag

{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}